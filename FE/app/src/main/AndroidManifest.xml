<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.argreeting">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ARGreeting"
        tools:targetApi="m">
        <activity
            android:name=".activity.viewGreeting.ViewGreetingActivity"
            android:exported="false"
            android:label="View Greeting"/>
        <activity
            android:name=".activity.sendGreeting.SendGreetingActivity"
            android:exported="false"
            android:label="Send a Greeting" />
        <activity
            android:name=".activity.signup.SignupActivity"
            android:exported="false"
            android:label="Sign Up" />
        <activity
            android:name=".activity.profile.ProfileActivity"
            android:exported="false"
            android:label="Profile" />
        <activity
            android:name=".activity.login.LoginActivity"
            android:exported="true"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">

        </activity>
    </application>

</manifest>